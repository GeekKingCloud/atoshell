#!/usr/bin/env bash
# ato — short alias for atoshell
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

exec bash "$REPO_DIR/atoshell.sh" "$@"
